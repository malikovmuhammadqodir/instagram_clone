import 'package:flutter/material.dart';

class Storubook extends StatefulWidget {
  const Storubook({super.key});

  @override
  State<Storubook> createState() => _StorubookState();
}

class _StorubookState extends State<Storubook> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: 
      Center(child:Text('storybook page'), )
      
    );
  }
} 